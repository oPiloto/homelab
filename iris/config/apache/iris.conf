<VirtualHost *:80>

    ServerName iris.onwire.com.br

    ############################################################
    # CSP
    ############################################################

    <Location "/csp">
        CSP On
        SetHandler csp-handler-sa
    </Location>

    <Location "/csp/bin">
        CSP On
        SetHandler csp-handler-sa
    </Location>

</VirtualHost>