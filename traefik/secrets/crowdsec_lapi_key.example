# Renomeie este arquivo para "crowdsec_lapi_key" (sem extensão) e substitua
# o conteúdo pela chave gerada com:
#
#   docker exec crowdsec cscli bouncers add traefik-bouncer
#
# Cole SOMENTE a chave (sem aspas, sem quebras de linha extras) abaixo.
# Este arquivo real (sem .example) está no .gitignore e nunca deve ser
# commitado no Git.
COLOQUE_A_CHAVE_AQUI
